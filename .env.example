# Scrapoxy Commander Credentials (for GUI at http://localhost:8889)
AUTH_LOCAL_USERNAME=admin
AUTH_LOCAL_PASSWORD=password

# JWT Secrets (Change these for production!)
BACKEND_JWT_SECRET=change_me_backend_secret_12345
FRONTEND_JWT_SECRET=change_me_frontend_secret_12345

# Ports
COMMANDER_PORT=8889
PROXY_PORT=8890

# Project Proxy Credentials
# These are generated by Scrapoxy when you create a project.
# 1. Run the stack.
# 2. Log in to http://localhost:8889.
# 3. Create a project.
# 4. Copy the Username and Password from the project settings.
# 5. Paste them here and restart the stack (or run ./run.sh again).
PROJECT_PROXY_USERNAME=
PROJECT_PROXY_PASSWORD=
