{
  "name": "scrapoxy-local-gateway-forwarder",
  "version": "1.0.0",
  "description": "No-auth sidecar forwarder for Scrapoxy Local Gateway",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "proxy-chain": "^2.3.0"
  }
}
